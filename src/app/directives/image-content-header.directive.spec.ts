import { ImageContentHeaderDirective } from './image-content-header.directive';

describe('ImageContentHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageContentHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
